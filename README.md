# Instalación


Crear un entorno virtual dentro de la carpeta del proyecto
```shell
virtualenv .venv
```

Acceder al entorno
```shell
source .venv/Scripts/activate
```
Instalar dependencias
```shell
pip install -r requirements.txt
```

# Lanzar aplicación
```shell
python main.py
```